# Default application settings
export EDITOR=nvim
export VISUAL=nvim

# Used by 'dev' function in functions directory to determine a base directory
# of development/code projects.
export DEV_ROOT=Users/"${USER}"/Development
